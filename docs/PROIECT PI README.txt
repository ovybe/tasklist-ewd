The folder needs to be extracted in C: to work without config changes.
If you already have xampp installed, just move the Tasklist folder into your xampp's htdocs folder.
To launch the site:
1. Launch xampp-control.exe
2. Start Apache and MySQL
3. Open the browser of your choice and write "localhost" in the URL